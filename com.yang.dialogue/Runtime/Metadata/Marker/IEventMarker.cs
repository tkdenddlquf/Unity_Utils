namespace Yang.Dialogue
{
    /// <summary>
    /// Marker interface for event keys. Implement this on a class that declares
    /// <c>const string</c> event keys, then assign that class to the DialogueSO so
    /// Event nodes can reference the keys it defines.
    /// </summary>
    public interface IEventMarker
    {

    }
}